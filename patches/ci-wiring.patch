diff --git a/.github/workflows/db-backup.yml b/.github/workflows/db-backup.yml
index c9c766e..c3d7c43 100644
--- a/.github/workflows/db-backup.yml
+++ b/.github/workflows/db-backup.yml
@@ -58,7 +58,7 @@ jobs:
             git config user.email "bot@stream-recorder.local"
             git add data/backups
             git commit -m "DB backup snapshot, $(date -u '+%Y-%m-%d')"
-            git push
+            bash scripts/safe-push.sh
             echo "✅ Backup committed."
           else
             echo "Nothing to commit."
diff --git a/.github/workflows/install-self-test.yml b/.github/workflows/install-self-test.yml
index 9561d45..d64b8cf 100644
--- a/.github/workflows/install-self-test.yml
+++ b/.github/workflows/install-self-test.yml
@@ -20,6 +20,7 @@ on:
       - ".github/workflows/install-self-test.yml"
       - "scripts/setup-warp.sh"
       - "scripts/utils.sh"
+      - "scripts/check-install-drift.sh"
   # Run daily so an upstream URL change (yt-dlp release moved, ytarchive
   # version gone, apt mirror dead) gets caught before the next live stream
   schedule:
@@ -122,7 +123,7 @@ jobs:
             echo "##  Install Self-Test"
             echo ""
             if (( ${#FAILED[@]} == 0 )); then
-              echo "**✅ All ${#FAILED[@]} tools installed cleanly**"
+              echo "**✅ All critical tools installed cleanly**"
             else
               echo "**❌ ${#FAILED[@]} tool(s) failed:** ${FAILED[*]}"
             fi
@@ -137,18 +138,14 @@ jobs:
       # Step 3: Structural drift check, ensure the install block in
       # stream-recorder.yml hasn't gained new commands that aren't replayed
       # here. Compares a fingerprint of the install lines.
+      # 2026-09-01: moved to scripts/check-install-drift.sh. The old inline
+      # version died under bash -eo pipefail whenever its awk anchors missed
+      # (step renames) → 13 straight days of false "install regression" pages
+      # (Aug 20 → Sep 1) while install was actually healthy. The script always
+      # exits 0: drift is a WARNING, never a failure.
       # ─────────────────────────────────────────────────────────────────────
       - name: "Drift check against the real workflow"
-        run: |
-          REAL=$(awk '/Install Dependencies/,/Start PoToken Provider/' .github/workflows/stream-recorder.yml \
-                 | grep -E "^\s*(sudo|pip3|curl|sed -i|chmod)" | wc -l)
-          TEST=$(awk '/Replay real install block/,/Verify all critical tools/' .github/workflows/install-self-test.yml \
-                 | grep -E "^\s*(sudo|pip3|curl|sed -i|chmod)" | wc -l)
-          echo "Real workflow install commands: $REAL"
-          echo "Self-test install commands:     $TEST"
-          if (( REAL > TEST + 1 )); then
-            echo "::warning::Real install block has $REAL commands but self-test only mirrors $TEST. Update install-self-test.yml so it stays representative."
-          fi
+        run: bash scripts/check-install-drift.sh
 
       - name: "Discord alert on failure"
         if: failure()
diff --git a/.github/workflows/quality-check.yml b/.github/workflows/quality-check.yml
index a3791fc..4bb515f 100644
--- a/.github/workflows/quality-check.yml
+++ b/.github/workflows/quality-check.yml
@@ -13,6 +13,7 @@ on:
     paths:
       - "dashboard/**"
       - "scripts/**"
+      - "tests/**"
       - ".github/workflows/**"
       - "package*.json"
   push:
@@ -20,6 +21,7 @@ on:
     paths:
       - "dashboard/**"
       - "scripts/**"
+      - "tests/**"
       - ".github/workflows/**"
       - "package*.json"
 
@@ -82,6 +84,17 @@ jobs:
       - name: "Recording cascade test"
         run: bash tests/record-cascade-test.sh
 
+      - name: "Regression tests"
+        run: |
+          failed=0
+          for f in tests/*.sh; do
+            echo "==== $f ===="
+            if ! bash "$f"; then
+              failed=1
+            fi
+          done
+          exit "$failed"
+
       - name: "Secret reference summary"
         run: |
           echo "## Referenced GitHub Secrets" >> "$GITHUB_STEP_SUMMARY"
diff --git a/.github/workflows/workflow-watchdog.yml b/.github/workflows/workflow-watchdog.yml
index e06c396..0b0c64f 100644
--- a/.github/workflows/workflow-watchdog.yml
+++ b/.github/workflows/workflow-watchdog.yml
@@ -19,6 +19,7 @@ on:
 
 permissions:
   contents: read
+  actions: read
 
 defaults:
   run:
@@ -49,32 +50,18 @@ jobs:
           sudo apt-get update -qq
           sudo apt-get install -y -qq curl jq
 
+      # 2026-09-01: moved to scripts/workflow-watchdog.sh. The old inline jq
+      # piped the critical-names array into index() and then read the run's
+      # path field off THAT array → "Cannot index array with string path",
+      # jq exited 5, and every hourly run since 2026-08-26 died before
+      # Discord. The script binds path from the run first.
       - name: "Inspect workflows"
+        env:
+          LOOKBACK_HOURS: ${{ github.event.inputs.lookback_hours || 6 }}
         run: |
           set -euo pipefail
-          LOOKBACK="${{ github.event.inputs.lookback_hours }}"
-          LOOKBACK="${LOOKBACK:-6}"
-          SINCE_EPOCH=$(date -u -d "${LOOKBACK} hours ago" +%s)
-          TOKEN="${GH_PAT:-${GITHUB_TOKEN:-}}"
-          if [[ -z "$TOKEN" ]]; then
-            echo "GH_PAT missing; cannot inspect workflow runs"
-            exit 0
-          fi
-
-          CRITICAL='stream-recorder.yml cookie-health.yml cloud-refresh.yml account-keepalive.yml youtube-to-archive.yml archive-to-mega.yml deploy-pages.yml quality-check.yml'
-          RUNS=$(curl -fsS \
-            -H "Authorization: Bearer ${TOKEN}" \
-            -H "Accept: application/vnd.github+json" \
-            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/runs?per_page=60")
-
-          FAILURES=$(echo "$RUNS" | jq -r --argjson since "$SINCE_EPOCH" --arg critical "$CRITICAL" '
-            .workflow_runs[]
-            | select((.status == "completed") and (.conclusion != "success") and (.conclusion != "cancelled"))
-            | select((.updated_at | fromdateiso8601) >= $since)
-            | select(($critical | split(" ")) as $c | $c | index(.path | split("/")[-1]))
-            | "- **" + .name + "**, `" + .conclusion + "`, " + .html_url
-          ')
-
+          LOOKBACK="${LOOKBACK_HOURS:-6}"
+          FAILURES=$(bash scripts/workflow-watchdog.sh)
           COUNT=$(printf '%s\n' "$FAILURES" | sed '/^$/d' | wc -l | tr -d ' ')
           echo "Found $COUNT recent critical failure(s)"
           {
diff --git a/.github/workflows/youtube-stats.yml b/.github/workflows/youtube-stats.yml
index 14c8e48..d49d671 100644
--- a/.github/workflows/youtube-stats.yml
+++ b/.github/workflows/youtube-stats.yml
@@ -122,6 +122,11 @@ jobs:
         run: |
           git config user.name "github-actions[bot]"
           git config user.email "github-actions[bot]@users.noreply.github.com"
-          git add data/youtube-stats.json public/yt-all.json
+          # The scrape step runs with working-directory: dashboard, so the
+          # snapshot lives at dashboard/public/yt-all.json — NOT public/yt-all.json
+          # (that pathspec matches nothing; the channel list never landed).
+          git add data/youtube-stats.json dashboard/public/yt-all.json
           git diff --staged --quiet || git commit -m "Auto-update YouTube stats + channel list [skip ci]"
-          git push
+          # Bare `git push` has been 100% red since 2026-08-26 (exit 128,
+          # "! [rejected] main -> main (fetch first)"). safe-push.sh rebases.
+          bash scripts/safe-push.sh
